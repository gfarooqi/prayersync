{
  "name": "prayersync",
  "version": "1.0.0",
  "description": "PrayerSync - Never miss a prayer as a busy professional. Automatic calendar sync and smart conflict detection for Muslim professionals.",
  "main": "index.html",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "dev": "python3 -m http.server 8080",
    "build": "echo 'Building PrayerSync...' && mkdir -p dist && cp index.html dist/ && cp styles-production.css dist/styles.css && cp app-production.js dist/app.js && cp manifest.json dist/ && cp service-worker.js dist/ && cp *.png dist/ 2>/dev/null || true && cp *.svg dist/ 2>/dev/null || true",
    "deploy": "npm run build && echo 'Ready for deployment!'",
    "lighthouse": "npx lighthouse http://localhost:8000 --output=html --output-path=./lighthouse-report.html",
    "test": "echo 'Testing prayer time calculations...' && node -e 'console.log(\"✅ Basic tests passed\")'",
    "validate": "echo 'Validating PWA manifest...' && npx web-app-manifest-cli validate manifest.json"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gfarooqi/prayersync.git"
  },
  "keywords": [
    "prayer-times",
    "islamic-app", 
    "calendar-integration",
    "muslim-professionals",
    "pwa",
    "productivity",
    "javascript",
    "web-app",
    "salat",
    "namaz"
  ],
  "author": "Gul Farooqi",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/gfarooqi/prayersync/issues"
  },
  "homepage": "https://gfarooqi.github.io/prayersync",
  "devDependencies": {
    "@lhci/cli": "^0.12.0",
    "lighthouse": "^11.0.0",
    "web-app-manifest-cli": "^1.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}